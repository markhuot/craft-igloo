{{ hiddenInput('elementId', elementId) }}
{{ hiddenInput('fieldHandle', fieldHandle) }}

<h2>Layout</h2>

<p><label class="font-bold">Number of Columns</label></p>
<p><input type="text" placeholder="1" class="text fullwidth" name="columns" value="{{ config.columns|default('') }}"></p>

<p><label class="font-bold">Resizable</label></p>
<p><input type="checkbox" name="resizable" value="1" {{ config.resizable|default(false)?'checked':'' }}></p>

<p><label class="font-bold">Snap to grid</label></p>
<p><input type="text" placeholder="12" class="text fullwidth" name="columns" value="{{ config.snapTo|default('') }}"></p>

<hr>

<p>
    You can customize the layout of any slot by providing custom HTML that will
    render in the Craft CP and allow users to fill in slots. For example, you may
    want to create a hero layout that provides two slots for a main hero and a smaller
    sidebar.
</p>

<textarea>
    {% verbatim %}
        <div class="flex flex-gap-4">
            <div>{{ slot('one') }}</div>
            <div>{{ slot('two') }}</div>
        </div>
    {% endverbatim %}
</textarea>
